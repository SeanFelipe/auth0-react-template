import React from 'react';

export default class Callback extends React.Component {
  render() {
    //alert("Callback render()")
    return (
      <div>
        Auth Callback loading screen
      </div>
    );
  }
}
